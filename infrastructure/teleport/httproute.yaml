apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: teleport
  namespace: teleport
spec:
  parentRefs:
    - name: shared-gateway
      namespace: gateway
  hostnames:
    - teleport.kube.it.com
  rules:
    - backendRefs:
        - name: teleport
          port: 3080