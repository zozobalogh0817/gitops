apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rabbitmq
  namespace: rabbitmq
spec:
  parentRefs:
    - name: shared-gateway
      namespace: gateway
  hostnames:
    - rabbit.kube.it.com
  rules:
    - backendRefs:
        - name: rabbitmq
          port: 15672