apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: minio
  namespace: minio
spec:
  parentRefs:
    - name: shared-gateway
      namespace: gateway
  hostnames:
    - minio.kube.it.com
  rules:
    - backendRefs:
        - name: minio-console
          port: 9001